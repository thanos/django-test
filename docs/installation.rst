============
Installation
============

At the command line::

    $ easy_install django-test

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-test
    $ pip install django-test
